{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then pnpm --filter @chore-wheel/database db:migrate; fi && next build",
  "crons": [
    {
      "path": "/api/jobs/assign-chores",
      "schedule": "0 8 * * *"
    }
  ]
}
