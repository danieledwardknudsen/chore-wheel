pnpm lint-staged

if command -v gitleaks &> /dev/null; then
  gitleaks protect --staged --redact --no-banner
else
  echo "⚠ gitleaks not installed — skipping deep secret scan (brew install gitleaks / winget install gitleaks)"
fi
